---
interface Props {
  title: string;
  date: Date;
  description: string;
  tags: string[];
  slug: string;
}

const { title, date, description, tags, slug } = Astro.props;
const formattedDate = date.toLocaleDateString('en-GB', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<article class="group rounded border-gray-50 border p-2.5">
  <div class="m-2">
  <a href={`/blog/${slug}`} class="block">
    <time class="text-sm text-gray-500 dark:text-gray-400" datetime={date.toISOString()}>
      {formattedDate}
    </time>
    <h3 class="mt-1 text-lg font-semibold dark:text-gray-100 text-gray-900 group-hover:text-blue-400 transition-colors">
      {title}
    </h3>
    <p class="mt-1 dark:text-gray-50 text-gray-600">{description}</p>
    {tags.length > 0 && (
      <div class="mt-2 flex flex-wrap gap-2">
        {tags.map((tag) => (
          <span class="rounded-full dark:bg-gray-800 bg-gray-100 px-2.5 py-0.5 text-xs dark:text-gray-50 text-gray-600">
            {tag}
          </span>
        ))}
      </div>
    )}
  </a>
  </div>
</article>
